import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wshan.brain_harmonics',
  appName: 'BrainHarmonics',
  webDir: 'dist'
};

export default config;
